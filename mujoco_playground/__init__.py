"""Minimal local runtime shim for the MJX environment and Brax wrapper.

The full playground distribution imports optional benchmark suites at package
import time. Section 40 only needs these runtime modules.
"""
