# Source this profile from Ubuntu WSL before running scripts/maintain.sh.
# The checkout and Docker bind-mounted artifacts will then live on WSL's
# native Linux filesystem rather than in a Windows/OneDrive-backed checkout.
export ASCENTO_INSTALL_DIR="/root/Ascento_MuJoCo_Playground"
export ASCENTO_COMPUTE="cu128"
